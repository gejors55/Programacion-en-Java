
public enum ENUM_COMANDO {
	HELP, QUIT, NEWINST, RUN, RESET, REPLACE;
}
	//private int numPalabras; // no se si hay que contar palabras aqui

	/*ENUM_COMANDO() {
		this(0);
	}

	ENUM_COMANDO(int palabras) {
		numPalabras = palabras;
	}

	public int getNumPalabras() {//si llamas a esto te dice cuantas palabras tiene la instruccion,
		return numPalabras; // 		numero de arriba (N)
	}
}
Nada de esto era necesario*/